# Spedu Labs 共用資源

此資料夾供新版子目錄共用。第一階段僅由 `math/`、`life/`、`info/` 引用，既有 `eng-115/`、`eng-grammar/`、`classroom/` 不變。

- `css/workspace.css`：統一標題列、側邊分類、卡片、搜尋與響應式外觀。
- `js/workspace.js`：分類篩選、搜尋、收藏與手機側欄。
- `images/`：預留共用圖片。
- `icons/`：預留共用圖示。
